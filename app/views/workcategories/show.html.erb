<% provide(:crumbs, "Works," + works_path) %>
<% provide(:title, @workcategory.name) %>

<% if @works.any? %>
	<%= @workcategory.description %>
  <%= render 'shared/table_works', works: @works%>
<% else %>
	<%= render 'shared/get_started_works' %>
<% end %>
