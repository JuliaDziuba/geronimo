<% provide(:crumbs, 'Works,' + works_path + "|" + @worksubcategory.workcategory.name + "," + workcategory_path(@worksubcategory.workcategory)) %>
<% provide(:title, @worksubcategory.name) %>

<% if @works.any? %>
	<%= @worksubcategory.description %>
  <%= render 'shared/table_works', works: @works %>
<% else %>
	<%= render 'shared/get_started_works' %>
<% end %>
