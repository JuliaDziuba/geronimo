<% provide(:title, 'Works > ' +  @worksubcategory.workcategory.name + ' > ' + @worksubcategory.name) %>
<h1><%= yield(:title) %></h1>

<% if @works.any? %>
  <%= render 'shared/table_works', works: @works %>
<% else %>
	<%= render 'shared/get_started_works' %>
<% end %>
