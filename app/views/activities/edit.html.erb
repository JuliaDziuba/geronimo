<% provide(:crumbs, 'Activities,' + activities_path 
	+ '|' + @activity.activitycategory.name + ',' + activitycategory_path(@activity.activitycategory)
	+ '|' + @activity.work.title + ',' + work_path(@activity.work)
) %>
<% provide(:title, 'Update' %>

<%= render 'shared/form_activity', activity: @activity %>