<% provide(:crumbs, "Works," + works_path + "|" + @activity.work.title + "," + work_path(@activity.work) + "|" + "Activities," + activities_path) %>
<% provide(:title, @activity.activitycategory.name) %>
<% provide(:delete_record, @activity) %>
<%= render 'form_activity', activity: @activity, activitycategories: @activitycategories, works: @works, venues: @venues, clients: @clients %>
