<% provide(:title, "Maker") %>
<div class="row-fluid">
	<div class="span3">
		<%= image_tag @user.image.url(:medium) %>
	</div>
	<div class="span6">
		<%= "#{@user.name} (#{@user.username})" %><br /> 
		<span class="lighter"> 
		</span><br />
		<i><%= @user.about %></i><br />
	</div>
</div><!-- row-fluid -->

