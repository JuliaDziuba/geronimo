<table>
	<caption><span class="h3"><%= "Sales to date by #{outcome}" %></span>: reported in 'dollars (# number of entries with data)' unless otherwise specified</caption>
  <thead>
    <tr>
    	<th><%= outcome.capitalize %></th>
    	<th>Number of sales</th>
      <th>Absolute retail value</th>
      <th>Absolute income</th>
      <th>Average income</th>
    </tr>
  </thead>
  <tbody>
    <% hoa.each do | k, v | %>
    	<tr>
    		<td><%= k %></td>
	    	<td><%= v.count %></td>
	    	<td><%= sumAndCountNonNilKeys(v, "retail") %></td>
	    	<td><%= sumAndCountNonNilKeys(v, "income") %></td>
	    	<td><%= averageAndCountNonNilKeys(v, "income") %></td>
    	</tr>
	   <% end %>
  </tbody>
</table>

