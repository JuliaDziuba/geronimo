<% provide(:page, 'New Action') %>
<% if not @type.nil? %>
	<% provide(:crumbs, "#{@type[:crumb_name]}," + eval(@type[:crumb_path]) + "|" + @crumb_name + "," + @crumb_path) %>
<% end %>
<% provide(:title, 'New') %>

<div class="row-fluuid">
	<div class="span5">
		<%= render 'form_action', action: @action_item, works: @works, venues: @venues, clients: @clients %>	
	</div>
</div>