<% if !venue.phone.to_s.blank? %>        
  <%= venue.phone.to_s + " | " %>
<% end %>
<% if !venue.email.blank? %>       
  <%= venue.email + " | " %>
<% end %>
<% if !venue.site.blank? %>        
  <%= venue.site %>
<% end %>