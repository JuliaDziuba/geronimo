<% if !venue.phone.to_s.empty? %>        
  <%= venue.phone.to_s + " | " %>
<% end %>
<% if !venue.email.empty? %>       
  <%= venue.email + " | " %>
<% end %>
<% if !venue.site.empty? %>        
  <%= venue.site %>
<% end %>