<% provide(:crumbs, 'Activities,' + activities_path 
	+ "|" + @venue.venuecategory.name + "," + venuecategory_path(@venue.venuecategory)
) %>
<% provide(:title, @category.name) %>

<% if @activities.any? %>
  <%= render 'shared/table_activities', activities: @activities %>
<% else %>
	<%= render 'shared/get_started_activities' %>
<% end %>
